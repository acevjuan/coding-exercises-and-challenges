function addTwo(num) {
    return num + 2;
}
;
function getUpper(val) {
    return val.toUpperCase();
}
;
function signUpUser(name, email, pin, isPaid) {
    if (isPaid === void 0) { isPaid = false; }
    return 'welcome!';
}
addTwo(5);
getUpper('juan');
